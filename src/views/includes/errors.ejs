<% if (errors.length > 0) { %>
<div class="row justify-content-center">
    <div class="col-lg-4">
        <div class="alert alert-danger">
        <% errors.forEach(error => { %>
            <%= error %><br>
        <% }); %>
        </div>
    </div>
</div>
<% } %>